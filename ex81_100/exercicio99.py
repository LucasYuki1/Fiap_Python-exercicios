def calcular_area_retangulo(base:float=1, altura:float=1) -> float:
    '''Retorna o calculo da area do retângulo'''
    print(f'Área do retângulo é: {base*altura}')
    total = base*altura
    return total
calcular_area_retangulo(10, 5)